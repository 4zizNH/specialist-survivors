// data/world.js
// Arena configuration. The world is intentionally much larger than any
// viewport so the camera has room to scroll. Tunable later.

export const WORLD = Object.freeze({
  width: 3200, // px — well beyond a typical screen
  height: 3200,
  tileSize: 64, // grid cell size in px
});
